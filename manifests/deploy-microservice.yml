kind: Deployment
apiVersion: apps/v1
metadata:
    labels:
        app: solo-microservice
        stack: frontend
        source: demo
    name: solo-microservice
    namespace: microservices
spec:
    replicas: 3
    selector:
        matchLabels:
            run: solo-microservice
    template:
        metadata:
            labels:
                app: solo-microservice
                run: solo-microservice
                source: demo
        spec:
            containers:
                -   image: 219099013464.dkr.ecr.us-west-2.amazonaws.com/solo-microservice
                    name: solo-microservice
                    ports:
                        -   name: web
                            containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
    labels:
        app: solo-microservice-service
    name: solo-microservice-service
    namespace: microservices
spec:
    selector:
        source: demo
        app: solo-microservice
    ports:
        -   name: web
            protocol: TCP
            port: 30201
            targetPort: 8080

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
    name: solo-ingress
    namespace: microservices
spec:
    rules:
        -   http:
                paths:
                    -   path: /solo(/|$)(.*)
                        backend:
                            serviceName: solo-microservice-service
                            servicePort: 30201


